#include <iostream>
using namespace std;
int main(){
    int num,sum=0;
    cout<<"Enter a number: ";
    cin>>num;
    sum+=num;
     cout<<"Enter another number: ";
    cin>>num;
        sum+=num;
     cout<<"Enter another number: ";
    cin>>num;
        sum+=num;
     cout<<"Enter another number: ";
    cin>>num;
        sum+=num;
     cout<<"Enter another number: ";
    cin>>num;
        sum+=num;
    cout<<"The result of sum of all the numbers is: "<<sum<<endl;
    
}